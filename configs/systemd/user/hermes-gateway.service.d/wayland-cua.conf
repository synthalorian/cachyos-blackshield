[Service]
# Enable cua-driver's experimental Wayland backend so computer_use screen
# capture + AX inspection work on the pure-Wayland Plasma session.
# (doctor 2026-08-11: X11 unreachable, XWayland path dead)
Environment="CUA_DRIVER_RS_ENABLE_WAYLAND=1"
